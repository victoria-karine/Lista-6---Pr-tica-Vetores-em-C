// Online C compiler to run C program online
#include <stdio.h>

int main() {
    
    int numeros [10];
    int i, valor, encontrado = 0;
    //Leia os valores
    for (pos = 0 pos < 10; pos++) {
    printf ("DIgite o nº da posição %d: ", pos);
    scanf ("%d", &numeros [pos]);
}
    
    printf ("Digite o valor a ser buscado: ");
    scanf ("%d", &valor);
    
    for (pos = 0 ; pos < 10; pos ++) {
        if (numeros [pos[ == valor ]])
        encontrou = 1; 
    }
    //Procure o valor
    
    if (encontrou) {
        printf ("Valor encontrado é %d\n");
        else
        
        printf ("Valor não encontrado!\n")
        
        return 0;

    }
