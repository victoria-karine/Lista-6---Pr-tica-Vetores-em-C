// Online C compiler to run C program online
#include <stdio.h>

int main() {
    
    int numeros [8];
    int i, soma = 0;
  //Leia os valores 
  
for (i = 0; pos < 8; i++) {
  printf("Digite o nº da posição %d\n", pos);
  scanf ("%d", &numeros[i]);
  
 
  for (pos = 0; pos < 8, pos++);
  {
      soma +=numos [pos];
  printf ("soma = %d\n", soma);
}

    return 0;
}
