// Online C compiler to run C program online
#include <stdio.h>

int main() {
    
    int numeros [8];
    int i, soma = 0;
  //Leia os valores 
  
for (pos = 0; pos < 8; pos++) {
  printf("Digite o nº da posição %d\n", pos);
  scanf ("%d", &numeros[pos]);
  
 
  for (pos = 0; pos < 8, pos++);
  {
      soma +=numos [pos];
  printf ("soma = %d\n", soma);
}

    return 0;
}
