// Online C compiler to run C program online
#include <stdio.h>

int main() {
    
    int numeros [12];
    int i, pares = 0;
  //Leia os valores 
  //Conte os pares
  
    for ( i = 0; i < 12; i++) {

   if (numeros[i] % 2 == 0) {
   printf("%d ", numeros[i]);
   scanf ("%d", &numeros[i]);
   }
 printf ("Quantidade de pares = %d\n", pares);
    }
    return 0;
}
